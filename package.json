{
  "name": "youtube-playlist-helper-monorepo",
  "private": true,
  "workspaces": [
    "projects/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm test --workspaces"
  },
  "devDependencies": {
    "svelte": "^5.0.0",
    "jsdom": "^24.0.0",
    "vitest": "^1.0.0"
  }
}
